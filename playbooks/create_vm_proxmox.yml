---
- name: Création d'une VM sur Proxmox via AWX
  hosts: localhost
  gather_facts: false
  vars:
    proxmox_host: "{{ proxmox_host }}"
    proxmox_user: "{{ proxmox_user | default('root@pam') }}"
    proxmox_password: "{{ proxmox_password }}"
    proxmox_node: "{{ proxmox_node }}"
    vm_name: "{{ vm_name }}"
    vm_id: "{{ vm_id | int }}"
    vm_cores: "{{ vm_cores | default(2) | int }}"
    vm_memory: "{{ vm_memory | default(2048) | int }}"
    vm_disk_size: "{{ vm_disk_size | default('20G') }}"
    vm_storage: "{{ vm_storage | default('local-lvm') }}"
    vm_os_template: "{{ vm_os_template }}"
    vm_ip: "{{ vm_ip }}"
    vm_gateway: "{{ vm_gateway }}"
    vm_netmask: "{{ vm_netmask | default(24) }}"
    vm_dns: "8.8.8.8"
    vm_vlan: ""
    vm_description: "VM creee via AWX"
    vm_tags: "awx"
    vm_start_at_boot: true
    vm_start_after_create: "{{ vm_start_after_create | default(true) | bool }}"
    ssh_public_key: "{{ ssh_public_key | default('') }}"

  tasks:

    # ---------------------------------------------------------------
    # 1. Validation des paramètres
    # ---------------------------------------------------------------
    - name: Vérification des paramètres obligatoires
      ansible.builtin.assert:
        that:
          - vm_name is defined and vm_name | length > 0
          - vm_id is defined and vm_id | int > 0
          - vm_ip is defined and vm_ip | length > 0
          - vm_gateway is defined and vm_gateway | length > 0
          - vm_os_template is defined and vm_os_template | length > 0
          - proxmox_host is defined and proxmox_host | length > 0
        fail_msg: "Un ou plusieurs paramètres obligatoires sont manquants."
        success_msg: "Paramètres validés avec succès."

    # ---------------------------------------------------------------
    # 2. Vérification de la disponibilité du VMID
    # ---------------------------------------------------------------
    - name: Vérification disponibilité du VMID {{ vm_id }}
      community.general.proxmox_kvm:
        api_host: "{{ proxmox_host }}"
        api_user: "{{ proxmox_user }}"
        api_password: "{{ proxmox_password }}"
        validate_certs: false
        node: "{{ proxmox_node }}"
        vmid: "{{ vm_id }}"
        state: current
      register: vm_check
      failed_when: false
      changed_when: false

    - name: Arrêt si le VMID est déjà utilisé
      ansible.builtin.fail:
        msg: "Le VMID {{ vm_id }} est déjà utilisé par une VM existante !"
      when: vm_check.status is defined

    # ---------------------------------------------------------------
    # 3. Clonage du template
    # ---------------------------------------------------------------
    - name: Clonage du template {{ vm_os_template }} → {{ vm_name }}
      community.general.proxmox_kvm:
        api_host: "{{ proxmox_host }}"
        api_user: "{{ proxmox_user }}"
        api_password: "{{ proxmox_password }}"
        validate_certs: false
        node: "{{ proxmox_node }}"
        newid: "{{ vm_id }}"
        name: "{{ vm_name }}"
        clone: "{{ vm_os_template }}"
        full: true
        storage: "{{ vm_storage }}"
        timeout: 300
        description: "{{ vm_description }}\nCréée via AWX le {{ lookup('pipe', 'date +%Y-%m-%d') }}"

    # ---------------------------------------------------------------
    # 4. Configuration CPU / RAM / boot
    # ---------------------------------------------------------------
    - name: Configuration CPU, RAM, options boot
      community.general.proxmox_kvm:
        api_host: "{{ proxmox_host }}"
        api_user: "{{ proxmox_user }}"
        api_password: "{{ proxmox_password }}"
        validate_certs: false
        node: "{{ proxmox_node }}"
        vmid: "{{ vm_id }}"
        cores: "{{ vm_cores }}"
        memory: "{{ vm_memory }}"
        onboot: "{{ vm_start_at_boot }}"
        tags: "{{ vm_tags }}"
        update: true

    # ---------------------------------------------------------------
    # 5. Redimensionnement du disque
    # ---------------------------------------------------------------
    - name: Redimensionnement disque → {{ vm_disk_size }}
      community.general.proxmox_disk:
        api_host: "{{ proxmox_host }}"
        api_user: "{{ proxmox_user }}"
        api_password: "{{ proxmox_password }}"
        validate_certs: false
        vmid: "{{ vm_id }}"
        disk: scsi0
        size: "{{ vm_disk_size }}"
        state: resized
      when: vm_disk_size is defined

    # ---------------------------------------------------------------
    # 6. Configuration réseau via cloud-init
    # ---------------------------------------------------------------
    - name: Configuration cloud-init
      community.general.proxmox_kvm:
        api_host: "{{ proxmox_host }}"
        api_user: "{{ proxmox_user }}"
        api_password: "{{ proxmox_password }}"
        validate_certs: false
        node: "{{ proxmox_node }}"
        vmid: "{{ vm_id }}"
        ipconfig:
          ipconfig0: "{{ 'ip=dhcp' if vm_ip == 'dhcp' else 'ip=' + vm_ip + '/' + (vm_netmask | string) + ',gw=' + vm_gateway }}"
        nameservers: "{{ vm_dns if vm_ip != 'dhcp' else omit }}"
        sshkeys: "{{ ssh_public_key }}"
        update: true

    # ---------------------------------------------------------------
    # 7. Configuration VLAN (optionnel)
    # ---------------------------------------------------------------
    - name: Configuration VLAN {{ vm_vlan }}
      community.general.proxmox_kvm:
        api_host: "{{ proxmox_host }}"
        api_user: "{{ proxmox_user }}"
        api_password: "{{ proxmox_password }}"
        validate_certs: false
        node: "{{ proxmox_node }}"
        vmid: "{{ vm_id }}"
        net:
          net0: "virtio,bridge=vmbr0,tag={{ vm_vlan }}"
        update: true
      when: vm_vlan | length > 0

    # ---------------------------------------------------------------
    # 8. Cycle SHUTDOWN et START
    # ---------------------------------------------------------------
    - name: Action SHUTDOWN (Extinction de la VM)
      community.general.proxmox_kvm:
        api_host: "{{ proxmox_host }}"
        api_user: "{{ proxmox_user }}"
        api_password: "{{ proxmox_password }}"
        validate_certs: false
        node: "{{ proxmox_node }}"
        vmid: "{{ vm_id }}"
        state: stopped
        timeout: 90
      when: vm_start_after_create | bool

    - name: Action START (Démarrage de la VM)
      community.general.proxmox_kvm:
        api_host: "{{ proxmox_host }}"
        api_user: "{{ proxmox_user }}"
        api_password: "{{ proxmox_password }}"
        validate_certs: false
        node: "{{ proxmox_node }}"
        vmid: "{{ vm_id }}"
        state: started
      when: vm_start_after_create | bool

    # ---------------------------------------------------------------
    # 9. Attente SSH
    # ---------------------------------------------------------------
    - name: Attente démarrage complet et accès SSH ({{ vm_ip }})
      ansible.builtin.wait_for:
        host: "{{ vm_ip }}"
        port: 22
        delay: 20
        timeout: 240
        state: started
      when:
        - vm_start_after_create | bool
        - vm_ip != "dhcp"
      register: ssh_wait
      failed_when: false

    # ---------------------------------------------------------------
    # 10. Ajout dynamique à l'inventaire
    # ---------------------------------------------------------------
    - name: Ajout de la VM à l'inventaire en mémoire
      ansible.builtin.add_host:
        name: "{{ vm_ip }}"
        groups: nouvelles_vms
        ansible_user: ansible
      when:
        - vm_start_after_create | bool
        - vm_ip != "dhcp"
        - ssh_wait.state is defined and ssh_wait.state == 'started'

    # ---------------------------------------------------------------
    # 11. Récapitulatif
    # ---------------------------------------------------------------
    - name: Récapitulatif
      ansible.builtin.debug:
        msg:
          - "======================================"
          - " VM créée, éteinte puis redémarrée !"
          - "======================================"
          - "Nom    : {{ vm_name }}"
          - "VMID   : {{ vm_id }}"
          - "IP     : {{ vm_ip }}"
          - "Statut : Cycle Shutdown/Start OK"
          - "======================================"
